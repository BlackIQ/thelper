// ----------------------------------------------
// index.js
// ----------------------------------------------
// Env.
// Just using a place to export our env

import dotenv from "dotenv";
dotenv.config();

const env = process.env;

export default env;
